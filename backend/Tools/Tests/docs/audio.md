# Audio Tests

Post-radio audio tests live in `Meta/Audio/`:

- backend/frontend playable-track policy alignment;
- invalid playback candidate classification;
- load-candidate filtering for `IsLoadingOverridden` imports;
- song log formatting for missing metadata;
- metadata merge behavior for cached and fetched song data, including preserved import flags.
